# 项目维护约定

使用中文与用户沟通，优先采用项目已有的 React、TypeScript、Vite 和 Node.js 技术栈。

## 分支与交付

- 每个独立功能从最新 main 创建独立分支；不要继续复用已合并的功能分支。
- 开发分支完成验证后通过 PR 合并；合并后同步本地 main。
- 用户要求构建时运行 npm run build；只有明确需要压缩包时运行 npm run package。

## 关联信息同步

产品名称是「蛋壳币价」，英文辅助名称是「Danke Coin」。产品改名、图标或版本调整时，主动检查并同步：

- public/manifest.json、popup.html、弹窗品牌和工具栏悬停文案。
- 图标源文件、public/icons 各尺寸资源、生成脚本与透明通道。
- README 的名称、图标、当前功能、版本和安装操作，以及相关验证记录。
- GitHub 仓库 About 简介与现有主页、发布信息中的关联名称；不要只修改源码。
- package.json 与锁文件的版本、构建产物和相关测试预期。

仓库路径、npm 工程标识和现有后台 alarm 标识不是产品展示名称。保留这些兼容标识；如需迁移，先核对已有安装和引用。历史文档保留原版本事实，不把历史验证结果改成新版本结果。
